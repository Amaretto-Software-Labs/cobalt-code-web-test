DATABASE_URL=postgresql://papier:papier_local_dev@127.0.0.1:5432/papier
# Optional local-only token hint shown on the unlock screen. Never set this to a production credential.
NEXT_PUBLIC_PAPIER_LOCAL_TOKEN=
# JSON object mapping deployment-provisioned bearer tokens to stable owner IDs.
# PAPIER_AUTH_TOKENS is intentionally omitted: do not commit authentication tokens.
