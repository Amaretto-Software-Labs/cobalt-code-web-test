services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: papier
      POSTGRES_USER: papier
      POSTGRES_PASSWORD: papier_local_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
